---
import ArrowUR from "./svgs/ArrowUR.astro";

interface Props {
    href: string;
    text: string;
}

const { href, text } = Astro.props;
---

<a href={href} class="inline-flex gap-1 font-medium drop-shadow-lg">
    {text}
    <ArrowUR styles="w-3 h-3 text-red-600" stroke="3" />
</a>
